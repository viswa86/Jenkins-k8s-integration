pipeline {
    agent any  // Runs on any available agent
    stages {
        stage('Clone Repository') {
            steps {
                git 'https://github.com/your-repo/sample-app.git'  // Replace with your repo
            }
        }
        stage('Build') {
            steps {
                echo 'Building the application... (Placeholder for actual build commands)'
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests... (Placeholder for actual test commands)'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying application to Kubernetes (Placeholder for deployment steps)'
            }
        }
    }
}
